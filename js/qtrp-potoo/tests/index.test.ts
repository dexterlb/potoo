import { expect } from 'chai';
import * as potoo from '../src';
const fs = eval('require("fs")');

describe('universe', () => {
  it('works', () => {
    let result = 6 * 7
    expect(result).equal(42)
  })
});
